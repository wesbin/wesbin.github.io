import * as React from "react"
import { Box } from "@material-ui/core"

const InfoBottom = ({ classes, data, pageContext }) => {

  return (
    <Box width={`100%`}>
    </Box>
  )
}

export default InfoBottom
